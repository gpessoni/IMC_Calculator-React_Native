import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    resultTitle: {
        color: "#ff0043",
        fontSize: 24,
        fontWeight: "bold",
      },
});

export default styles;
