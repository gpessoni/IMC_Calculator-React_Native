import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    resultTitle: {
        color: "#ff0043",
        fontSize: 20,
        fontWeight: "bold",
      },
});

export default styles;
